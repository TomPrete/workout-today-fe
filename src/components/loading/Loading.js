import React from 'react';
import './Loading.css'

const Loading = (props) => {
  console.log('here')
  return (
    <div className='loader-container'>
      <div className='loader'></div>
    </div>
  );
};

export default Loading;
